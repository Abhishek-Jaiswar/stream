<div class="movie-detail">
    <img src="<%= movie.image %>" alt="<%= movie.title %>">
    <h1><%= movie.title %> (<%= movie.year %>)</h1>
    <p><%= movie.description %></p>
    <p>Rating: <%= movie.rating %></p>
    <p>Director: <%= movie.director %></p>
    <!-- Add more movie-specific details as needed -->
</div>
  